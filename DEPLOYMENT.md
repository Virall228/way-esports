# Deployment Guide for WAY-Esports Telegram Mini App

Этот документ описывает процесс полной настройки и деплоя проекта WAY-Esports для работы в Telegram Mini App.

## 🚀 Быстрая навигация

- **Деплой на облачный сервер (Backend + Frontend вместе)** → см. [DEPLOY_CLOUD_SERVER.md](./DEPLOY_CLOUD_SERVER.md)
- **Быстрый старт для облачного сервера** → см. [QUICK_START_CLOUD.md](./QUICK_START_CLOUD.md)
- **Раздельный деплой (Backend и Frontend отдельно)** → см. инструкции ниже

## 📋 Предварительные требования

1. **Node.js** версии 18+ и npm/yarn
2. **MongoDB** база данных (локальная или облачная, например MongoDB Atlas)
3. **Telegram Bot Token** от [@BotFather](https://t.me/botfather)
4. **Домен** для размещения фронтенда и бекенда (или использование сервисов типа Railway, Vercel, Heroku)

## 🔧 Настройка Backend

### 1. Установка зависимостей

```bash
cd backend
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `backend/` на основе `env.example`:

```env
# Server Configuration
PORT=3000
NODE_ENV=production

# MongoDB
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/way-esports?retryWrites=true&w=majority

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
BOOTSTRAP_ADMIN_TELEGRAM_ID=your_telegram_id_here

# CORS Configuration
# Для Telegram Mini App используйте * или укажите ваш домен
CORS_ORIGIN=*

# Admin Configuration
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-password

# Email Configuration (опционально)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
EMAIL_SECURE=false
```

### 3. Сборка и запуск Backend

```bash
# Сборка TypeScript
npm run build

# Запуск в production
npm start
```

Или для разработки:
```bash
npm run dev
```

## 🎨 Настройка Frontend

### 1. Установка зависимостей

```bash
cd way-esports-front/frontend
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `way-esports-front/frontend/`:

```env
# API Configuration
# Укажите URL вашего бекенда
VITE_API_URL=https://api.yourdomain.com

# App Configuration
VITE_APP_NAME=WAY-Esports
VITE_APP_VERSION=1.0.0
```

### 3. Сборка Frontend

```bash
# Production сборка
npm run build
```

Собранные файлы будут в папке `dist/`.

## 🤖 Настройка Telegram Bot

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните полученный токен в `.env` файл бекенда

### 2. Настройка Mini App

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/newapp` и выберите вашего бота
3. Укажите название приложения
4. **Важно**: Укажите URL вашего фронтенда (например: `https://yourdomain.com`)
5. Загрузите иконку приложения

### 3. Настройка Webhook (опционально)

Если вы используете webhook для бота:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -d "url=https://api.yourdomain.com/api/webhook"
```

## 🚀 Деплой

### Вариант 1: Railway (Рекомендуется)

#### Backend:
1. Создайте аккаунт на [Railway](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Выберите папку `backend/`
5. Добавьте все переменные окружения из `.env`
6. Railway автоматически определит Node.js и запустит `npm start`

#### Frontend:
1. Создайте новый сервис в том же проекте
2. Выберите папку `way-esports-front/frontend/`
3. Добавьте переменные окружения:
   - `VITE_API_URL=https://your-backend-url.railway.app`
4. Railway автоматически соберет и задеплоит фронтенд

### Вариант 2: Vercel (Frontend) + Railway (Backend)

#### Frontend на Vercel:
1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Импортируйте репозиторий
3. Укажите корневую папку: `way-esports-front/frontend`
4. Добавьте переменные окружения
5. Vercel автоматически соберет проект

#### Backend на Railway:
Следуйте инструкциям выше для Railway

### Вариант 3: Собственный сервер (VPS)

#### Backend:
```bash
# На сервере
cd /var/www/way-esports/backend
npm install
npm run build

# Используйте PM2 для управления процессом
npm install -g pm2
pm2 start dist/index.js --name way-esports-backend
pm2 save
pm2 startup
```

#### Frontend:
```bash
# На сервере
cd /var/www/way-esports/way-esports-front/frontend
npm install
npm run build

# Используйте nginx для раздачи статических файлов
# См. конфигурацию nginx ниже
```

#### Nginx конфигурация для Frontend:

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    root /var/www/way-esports/way-esports-front/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кеширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### Nginx конфигурация для Backend (Reverse Proxy):

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## ✅ Проверка работоспособности

### 1. Проверка Backend

```bash
# Health check
curl https://api.yourdomain.com/api/health

# Должен вернуть:
# {"status":"ok","mongo":"connected","timestamp":"..."}
```

### 2. Проверка Frontend

1. Откройте ваш домен в браузере
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что API запросы идут на правильный URL

### 3. Проверка Telegram Mini App

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку "Open App" или отправьте `/start`
3. Приложение должно открыться
4. Проверьте аутентификацию через Telegram

## 🔒 Безопасность

### Рекомендации:

1. **Никогда не коммитьте `.env` файлы** в Git
2. Используйте **сильные JWT секреты** (минимум 32 символа)
3. Настройте **HTTPS** для всех доменов
4. Используйте **CORS** правильно (не `*` в production, если возможно)
5. Настройте **rate limiting** на бекенде
6. Регулярно обновляйте зависимости

## 📝 Чеклист перед деплоем

- [ ] Все переменные окружения настроены
- [ ] MongoDB база данных создана и доступна
- [ ] Telegram бот создан и токен добавлен
- [ ] Mini App настроен в BotFather с правильным URL
- [ ] Backend собран и запущен
- [ ] Frontend собран и задеплоен
- [ ] CORS настроен правильно
- [ ] HTTPS настроен (для production)
- [ ] Health check эндпоинт работает
- [ ] Аутентификация через Telegram работает
- [ ] Админка доступна и работает

## 🐛 Решение проблем

### Проблема: CORS ошибки

**Решение**: Убедитесь, что `CORS_ORIGIN` в backend `.env` включает ваш домен или установлен в `*` для Telegram Mini App.

### Проблема: Telegram аутентификация не работает

**Решение**: 
1. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
2. Убедитесь, что initData отправляется в заголовке `X-Telegram-Data`
3. Проверьте логи бекенда на наличие ошибок валидации

### Проблема: Данные не сохраняются в базу

**Решение**:
1. Проверьте подключение к MongoDB
2. Убедитесь, что все модели правильно экспортированы
3. Проверьте логи бекенда на наличие ошибок

### Проблема: Frontend не может подключиться к API

**Решение**:
1. Проверьте `VITE_API_URL` в `.env` файле фронтенда
2. Убедитесь, что URL правильный и доступен
3. Проверьте CORS настройки на бекенде

## 📞 Поддержка

Если у вас возникли проблемы, проверьте:
1. Логи бекенда
2. Консоль браузера
3. Network tab в DevTools
4. Логи сервера (если используете VPS)

---

**Готово!** Ваше приложение должно работать в Telegram Mini App. 🎉

