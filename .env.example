# Database Configuration (adjust for your setup; dev assumes external Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=way
DB_USER=postgres
DB_PASSWORD=scam1516

# Redis Configuration (adjust for your setup; dev assumes external Redis)
REDIS_HOST=localhost
REDIS_PORT=6379

# Application Configuration
NODE_ENV=development
PORT=3000

# JWT Secret (generate a strong one for production)
JWT_SECRET=your_super_secret_jwt_key_here_change_me

# Email Service (if used)
EMAIL_HOST=vb917185@gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Other (add as needed, e.g., for payments, API keys)
STRIPE_SECRET_KEY=sk_test_...
TELEGRAM_BOT_TOKEN=your_bot_token
