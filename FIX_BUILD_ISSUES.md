# 🔧 Исправления проблем сборки

## ✅ Исправленные проблемы

### 1. Ошибка с terser
**Проблема:** `Error: terser not found. Since Vite v3, terser has become an optional dependency.`

**Решение:**
- Переключено с `minify: 'terser'` на `minify: 'esbuild'` в `vite.config.ts`
- `esbuild` встроен в Vite и не требует дополнительных зависимостей
- Быстрее и надежнее для production сборки

### 2. Синтаксическая ошибка в Dockerfile
**Проблема:** `if [ -f package-lock.json);` - неправильный синтаксис

**Решение:**
- Упрощена логика установки зависимостей
- Теперь всегда используется `npm install` вместо условного `npm ci`
- `npm install` автоматически синхронизирует `package-lock.json` с `package.json`

### 3. Несинхронизированные package.json и package-lock.json
**Проблема:** `npm ci` требует точного соответствия между файлами

**Решение:**
- Dockerfile теперь использует `npm install`, который более гибкий
- Автоматически обновляет lock файл при необходимости

## 🚀 Пересборка на сервере

После применения исправлений выполните на сервере:

```bash
cd /opt/way-esports  # или путь к вашему проекту

# Остановка контейнеров
docker-compose down

# Пересборка с очисткой кеша
docker-compose build --no-cache web

# Запуск
docker-compose up -d
```

Или полная пересборка всех сервисов:

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 📝 Опционально: Обновление package-lock.json локально

Если хотите вернуться к использованию `npm ci` (быстрее и надежнее для CI/CD):

```bash
cd way-esports-front/frontend
rm package-lock.json
npm install
git add package-lock.json
git commit -m "Update package-lock.json"
```

Затем обновите Dockerfile обратно на `npm ci`:

```dockerfile
RUN npm ci --ignore-scripts --no-audit --no-fund
```

## ✅ Проверка

После пересборки проверьте:

```bash
# Логи сборки
docker-compose logs web

# Статус контейнеров
docker-compose ps

# Проверка работы
curl http://localhost/api/health
```

---

**Готово!** Сборка должна пройти успешно. 🎉
