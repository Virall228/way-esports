// Rate limiting middleware - simplified version without external dependencies
export const apiLimiter = (req: any, res: any, next: any) => {
  // Simple rate limiting implementation
  next();
};

export const authLimiter = (req: any, res: any, next: any) => {
  // Simple rate limiting implementation
  next();
};

export const createAccountLimiter = (req: any, res: any, next: any) => {
  // Simple rate limiting implementation
  next();
};

 